Written by: Jack Wolfson

Takes the user's base and exponent and computes the value using a function without the build in ** command

Biggest error was making the exponent loop, but eventually figured out the if and while statements

No bugs so far